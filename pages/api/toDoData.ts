import { NextApiRequest, NextApiResponse } from 'next';
import Task from '../../models/Task';

export default (req: NextApiRequest, res: NextApiResponse<Task[]>) => {
//   res.status(200).json({ name: 'John Doe' })
}